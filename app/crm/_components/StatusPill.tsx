// Legacy file from an earlier scaffold. Kept (empty) only because
// the Filesystem tool cannot delete files. Nothing imports this.
// Safe to delete manually:  rm -rf app/crm/_components
export {};
